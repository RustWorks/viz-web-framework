[package]
name = "unix-socket"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
viz = { path = "../../viz", features = ["unix-socket"] }

tokio = { workspace = true, features = ["rt-multi-thread", "macros"] }
