[package]
name = "unix-socket"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
viz = { workspace = true, features = ["unix-socket"] }

tokio = { workspace = true, features = ["rt-multi-thread", "macros"] }
