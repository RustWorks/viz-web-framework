[package]
name = "websocket-chat"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
viz = { path = "../../viz", features = ["websocket"] }

tokio = { version = "1.0", features = ["rt", "rt-multi-thread", "macros"] }
futures-util = "0.3.21"
